<template>
    <div>
        <h1>User Edit</h1>
        <p>Locale: {{ $route.query.locale }}</p>
        <p>Analytics : {{ $route.query.q }}</p>
        <button @click="confrimed = true">Confirm</button>
        <div  style="height: 700px;"></div>
        <p id="data">Some Data!</p>
    </div>
</template>

<script>
export default {
    data() {
        return {
            confirmed: false
        }
    },
    beforeRouteLeave(to, from, next) {
        if (this.confirmed) {
            next()
        } else {
            if (confirm('Are you sure?')) {
                next()
            } else {
                next()
            }
        }
        next()
    }
}
</script>